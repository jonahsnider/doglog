diff --git a/providers/changeset.ts b/providers/changeset.ts
--- a/providers/changeset.ts
+++ b/providers/changeset.ts
@@ -1,11 +1,13 @@
 import type { LoaderContext } from 'astro/loaders'
 import { z } from 'astro/zod'

-import { loadMarkdownData, type MarkdownProviderConfig } from './markdown'
-
-import { ProviderBaseConfigSchema } from '.'
+import {
+  loadMarkdownData,
+  MarkdownProviderBaseConfigSchema,
+  type MarkdownProviderConfig,
+} from './markdown'

-export const ChangesetProviderConfigSchema = ProviderBaseConfigSchema.extend({
+export const ChangesetProviderConfigSchema = MarkdownProviderBaseConfigSchema.extend({
   /**
    * The path or URL of the changelog file generated by Changeset to load.
    *
diff --git a/providers/keep-a-changelog.ts b/providers/keep-a-changelog.ts
--- a/providers/keep-a-changelog.ts
+++ b/providers/keep-a-changelog.ts
@@ -1,11 +1,13 @@
 import type { LoaderContext } from 'astro/loaders'
 import { z } from 'astro/zod'

-import { loadMarkdownData, type MarkdownProviderConfig } from './markdown'
-
-import { ProviderBaseConfigSchema } from '.'
+import {
+  loadMarkdownData,
+  MarkdownProviderBaseConfigSchema,
+  type MarkdownProviderConfig,
+} from './markdown'

-export const KeepAChangelogProviderConfigSchema = ProviderBaseConfigSchema.extend({
+export const KeepAChangelogProviderConfigSchema = MarkdownProviderBaseConfigSchema.extend({
   /**
    * The path or URL of the changelog file using the Keep a Changelog format to load.
    *
diff --git a/providers/markdown.ts b/providers/markdown.ts
--- a/providers/markdown.ts
+++ b/providers/markdown.ts
@@ -15,8 +15,27 @@
 import type { VersionDataEntry } from '../loader/schema'
 import { slugifyVersion } from '../loader/utils'

-import type { ProviderBaseConfigSchema } from '.'
+import { ProviderBaseConfigSchema } from '.'

+export const MarkdownProviderBaseConfigSchema = ProviderBaseConfigSchema.extend({
+  /**
+   * An optional function called for every version entry to derive its publish date from the version title.
+   *
+   * The function receives the original version title before it is passed to `process`.
+   */
+  extractDate: z
+    .function({
+      input: [
+        z.object({
+          /** The original version title found in the changelog. */
+          title: z.string(),
+        }),
+      ],
+      output: z.union([z.date(), z.undefined(), z.void()]),
+    })
+    .optional(),
+})
+
 const urlRegex = /^https?:\/\//

 export async function loadMarkdownData(config: MarkdownProviderConfig, context: LoaderContext) {
@@ -143,6 +162,7 @@
   index: number,
 ): VersionDataEntry | undefined {
   let title = version.title
+  const date = config.extractDate?.({ title })

   const process = config.process ?? config.markdown.process

@@ -158,6 +178,7 @@
     id,
     body: toMarkdown({ type: 'root', children: version.nodes as RootContent[] }),
     base: config.base,
+    date,
     index,
     provider: config.provider,
     slug,
@@ -165,7 +186,7 @@
   }
 }

-export interface MarkdownProviderConfig extends z.output<typeof ProviderBaseConfigSchema> {
+export interface MarkdownProviderConfig extends z.output<typeof MarkdownProviderBaseConfigSchema> {
   /**
    * The path or URL of the Markdown changelog file to load.
    *
diff --git a/providers/markdown.ts b/providers/markdown.ts
--- a/providers/markdown.ts
+++ b/providers/markdown.ts
@@ -3,7 +3,7 @@

 import { getConditionalHeaders, storeConditionalHeaders } from '@ascorbic/loader-utils'
 import type { LoaderContext } from 'astro/loaders'
-import type { z } from 'astro/zod'
+import { z } from 'astro/zod'
 import type { Node, RootContent } from 'mdast'
 import { fromMarkdown } from 'mdast-util-from-markdown'
 import { toMarkdown } from 'mdast-util-to-markdown'
